# Legacy Python live examples

NT v2 compatibility note: Python live `TradingNode` content below is
migration/reference-only; use Rust `LiveNode` for new work.

The former `references/examples/live/` aliases pointed at quarantined Python
`TradingNode` executables owned by `nt-adapters`. They were removed from the
active namespace during the NT V2 Rust cutover. Use
`skills/nt-adapters/references/examples/legacy_migration/` only when migrating
an existing Python deployment; new live work uses Rust `LiveNode` guidance.
